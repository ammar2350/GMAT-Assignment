function Profile (props) {
    return (
        <div>
            <p><b>Nama</b> : {props.nama}</p>
            <p><b>NIM</b> : {props.nim}</p>
        </div>
    );
}

export default Profile